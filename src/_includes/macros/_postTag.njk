{% macro postTag(params) %}
  <div class="mt-4 flex gap-2">
    {% for tag in params %}
      <span class="inline-flex items-center gap-1 rounded-full bg-{{ tag.colour }}-50 px-2 py-1 text-xs font-semibold text-{{ tag.colour }}-600">{{ tag.title }}</span>
    {% endfor %}
  </div>
{% endmacro %}